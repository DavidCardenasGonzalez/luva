{
  "name": "luva-infra",
  "version": "0.1.0",
  "private": true,
  "bin": {
    "luva-infra": "bin/luva-infra.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "cdk": "cdk",
    "synth": "zsh -lc 'export PATH=/Users/cardenas/.nvm/versions/node/v20.19.0/bin:$PATH && set -a && [ -f .env ] && source .env && set +a && node_modules/.bin/cdk synth'",
    "deploy": "zsh -lc 'export PATH=/Users/cardenas/.nvm/versions/node/v20.19.0/bin:$PATH && set -a && [ -f .env ] && source .env && set +a && node_modules/.bin/cdk deploy --profile david'"
  },
  "devDependencies": {
    "@types/node": "^18",
    "aws-cdk": "^2.1121.0",
    "esbuild": "^0.21.5",
    "source-map-support": "^0.5.21",
    "ts-node": "^10",
    "typescript": "^5"
  },
  "dependencies": {
    "aws-cdk-lib": "2.240.0",
    "constructs": "^10.3.0"
  }
}
